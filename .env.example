VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_DB_PASSWORD=

# Supabase Edge Functions (remote): set via `supabase secrets set ...`
# Local/dev: export in your shell when running `supabase functions serve`
#
# This app uses the OpenAI Chat Completions wire format with Bearer auth.
# Officially supported: OpenAI, and OpenAI-compatible endpoints that accept the
# same request shape + Bearer auth (e.g. Gemini OpenAI compatibility endpoint).
#
# OpenAI
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_FACTS_MODEL=gpt-5-nano
LLM_RENDER_MODEL=gpt-5-nano
LLM_MAX_TOKENS=1200
LLM_TEMPERATURE=0.4
LLM_TIMEOUT_MS=20000
LLM_REASONING_EFFORT=minimal
#
# Gemini (OpenAI compatibility endpoint)
# LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# LLM_API_KEY=
# LLM_FACTS_MODEL=gemini-3-flash-preview
# LLM_RENDER_MODEL=gemini-3-flash-preview
